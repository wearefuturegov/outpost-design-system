import { Meta } from '@storybook/addon-docs';
import { Template } from './template'
import { pathRoot, title } from './config'

<Meta title={`${pathRoot}/${title}/Intro`} component={Template} />

# Button

<style>
  {`
    .example {
      border: 1px solid red;
    }
  `}
</style>

# Example Button

This is the readme page for the Example button, since its mdx we can do fun things:

<code>ExampleButton.stories.mdx</code>

<br />

<div className="example">
<Template label="Text" />
</div>


<br />


## Story structure

* Playground - add edit amend options here
* Features - Use this to highlight certain features components have
* Standards - if there are any standard configurations we use for things then inlude them in this section


## Badge options

* "beta", "deprecated", "stable" & "releaseCandidate"