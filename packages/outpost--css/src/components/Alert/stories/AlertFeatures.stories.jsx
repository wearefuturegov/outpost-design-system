import {AlertTemplate} from './AlertTemplate'
import { AlertPathRoot, AlertTitle } from './AlertConfig'
export default {
  title: `${AlertPathRoot}/${AlertTitle}/Features`
}

// export const Disabled = AlertTemplate.bind({})
// Disabled.storyName = 'Disabled'
// Disabled.args = {
//   label: 'Disabled',
//   disabled: true
// }
