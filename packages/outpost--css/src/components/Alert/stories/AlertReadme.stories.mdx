import { Meta } from '@storybook/addon-docs';
import { AlertTemplate } from './AlertTemplate'
import { AlertPathRoot, AlertTitle } from './AlertConfig'

<Meta title={`${AlertPathRoot}/${AlertTitle}/Intro`} component={AlertTemplate} />

# Alert

This has an external dependency and needs more work

TODO - bring in css from `@reach/alert`